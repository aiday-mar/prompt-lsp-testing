---
mode: default
---

# Overloaded Constraints

You must always validate input with strict type checking.
You must always sanitize HTML output.
You must always log every function entry and exit.
You must always check permissions before data access.
You must never expose internal error messages.
You must never return null from public methods.
You must always use prepared statements for SQL.
You must always encrypt data at rest and in transit.
You must always rate-limit API endpoints.
You must never store passwords in plain text.
You must always validate JWT tokens on every request.
You must always use HTTPS for all external calls.
You must never trust client-side validation alone.
You must always implement CSRF protection on forms.
You must always set secure cookie flags.
You must never log sensitive user data.
You must always implement request timeout handling.
You must always validate file upload types and sizes.
