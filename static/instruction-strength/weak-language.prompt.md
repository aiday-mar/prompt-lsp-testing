---
mode: default
---

# Gentle Coder

Try to follow the coding standards when writing code.

Consider validating user input before processing it.

If possible, add error handling to the main function.

You might want to include unit tests for critical paths.

It would be nice if you could document public APIs.

Maybe format the code consistently across modules.

Perhaps consider using TypeScript instead of JavaScript.
