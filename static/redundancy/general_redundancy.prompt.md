---
description: Prompt combining duplicate instructions and subsumption
---

# Redundant Rules

## Security Section

Look at the data and validate before processing. 

Always validate user input before processing any data.

Never expose internal error messages to end users.

## Validation Section

Always validate user input before processing any data.

Ensure all incoming data is sanitized before use.

## Error Handling Section

Never expose internal error messages to end users.

Log all errors with timestamps and stack traces.

## Summary

Always validate user input before processing any data.

# Subsumed Constraints

Never use eval() in any generated code.

Avoid using eval() when possible.

Never include hardcoded credentials in responses.

Avoid including secrets or credentials in code.

Never return raw SQL queries to the user.

Avoid exposing SQL queries in responses.

Never use synchronous file I/O in server code.

Avoid blocking I/O operations in request handlers.
